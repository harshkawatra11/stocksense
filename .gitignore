# data (regenerable via research/backfill.py)
data_store/

# python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
venv/

# secrets
.env
*.env.local

# personal financial data (real broker exports -- account numbers, trade
# history). Anchored to repo root with a leading slash -- unanchored
# "statements/" also matched src/stocksense/statements/, the SOURCE
# CODE package, and silently excluded it from every future `git add`.
/statements/

# run logs (local scratch output from backfill/serve commands)
*.log

# OS
.DS_Store
Thumbs.db

# desktop (Electron)
desktop/node_modules/
desktop/dist/

# UI reference screen recordings (large binaries, kept locally under docs/reference/ui/)
*.mp4
*.mkv
*.mov
*.webm

# data spine (regenerable via the backfill CLI)
data_store/
*.duckdb
*.duckdb.wal
*.parquet

# probe outputs may contain account identifiers
research/probes/*.local.md

# node
node_modules/
dist/
