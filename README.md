# StockSense

A trading system for the NSE. The desktop frontend (`desktop/`) is built and unchanged.
The backend — data pipeline, models, evaluation, and execution — is being rebuilt from
scratch on this branch with a new quant approach.

## What's here

```
desktop/    Electron desktop frontend (unchanged)
```

The backend will be documented here as it's built.
