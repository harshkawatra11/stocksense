"""
Portfolio construction — the structural addition over v1's shortlist
approach (see the build plan). Turns ranked scores into target weights
under a turnover budget, with a no-trade band so "hold" is a real,
reachable outcome rather than something the model never emits.
"""
